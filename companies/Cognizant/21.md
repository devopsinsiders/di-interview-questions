# 21 Questions at Cognizant

* **Question:** What is a Terraform data block, and how do you use it to reference existing Azure resources in

* **Question:** your code? If one VM becomes corrupted after deployment, how would you redeploy only that VM using

* **Question:** Terraform? If the Terraform state file gets corrupted, how would you recover it, and what are the best

* **Question:** practices to protect the state file? How do you securely manage sensitive information such as passwords, keys, and certificates in

* **Question:** Terraform, and how do you integrate Azure Key Vault? How would you import existing Azure resources into Terraform so they can be managed as

* **Question:** Infrastructure as Code? Have you worked with Azure Verified Modules (AVM) or Cloud Adoption Framework (CAF)

* **Question:** Terraform modules? How do you structure your Terraform projects using root and child modules, and do you build your

* **Question:** own modules or reuse existing ones? How would you structure your Terraform code using root modules, child modules, locals.tf,

* **Question:** for_each, and tfvars to follow the DRY (Don't Repeat Yourself) principle, so that common resources (Key Vault, Recovery Services Vault, Resource Group, etc.) don't need to be defined for every VM? Which Terraform meta-arguments support key-value pairs, and when would you use for_each,

* **Question:** count, map, or nested maps? If you need temporary access to a Storage Account owned by another team, but they cannot grant

* **Question:** you RBAC permissions, how would you securely access the Storage Account? Explain the use of SAS Tokens and when you would choose them over RBAC. Suppose a resource was created manually in Azure. How will you manage it using Terraform?

* **Question:** What is Terraform Drift, and how do you detect and safely handle manual changes made through

* **Question:** the Azure Portal before running terraform plan and terraform apply? Suppose an application team requests a new VM. What would be your end-to-end approach for

* **Question:** provisioning the VM using reusable Terraform modules and handing it over to the application team? Have you worked with Azure Private Endpoints? Suppose a VM suddenly loses connectivity to a

* **Question:** Storage Account over a Private Endpoint after a Terraform or network change. How would you troubleshoot the issue, including verifying Private DNS Zone resolution, NSGs, UDRs, firewall rules, port 443, and other network configurations? How do you design reusable Terraform modules for multiple environments (Dev, UAT, Prod)?

* **Question:** What are Terraform Workspaces, why used, benefits, when instead of separate environment

* **Question:** folders? How would you plan and execute a Terraform version upgrade (e.g., 0.x to 1.x), compatibility

* **Question:** checks? What is the purpose of null_resource in Terraform, practical use case?

* **Question:** What is a Terraform data block, how do you use it to reference existing Azure resources?

* **Question:** If the Terraform state file gets corrupted, how would you recover it, best practices to protect state

* **Question:** file? How do you securely manage sensitive information (passwords, keys, certs) in Terraform, integrate

* **Question:** Azure Key Vault? How would you import existing Azure resources into Terraform?

* **Question:** What is Terraform Drift, how detect and safely handle manual changes made through Azure Portal

* **Question:** before plan/apply? How do you structure Terraform projects using root and child modules, build own or reuse

* **Question:** existing?

