# 22 Questions at Cognizant

* **Question:** Terraform modules and handing it over. How structure Terraform code using root modules, child modules, locals.tf, for_each, tfvars to

* **Question:** follow DRY principle? Which Terraform meta-arguments support key-value pairs - for_each, count, map, nested maps?

* **Question:** VM suddenly loses connectivity to Storage Account over Private Endpoint after Terraform/network

* **Question:** change - troubleshoot Private DNS Zone, NSGs, UDRs, firewall rules, port 443.

