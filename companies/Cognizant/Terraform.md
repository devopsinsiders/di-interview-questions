# Terraform Questions at Cognizant

--> Rate yourself on Terraform.

--> What is the difference between the Terraform block (required_providers) and the Provider block?

--> Explain the Terraform Import process.

--> Which files are required while importing existing resources into Terraform?

--> How would you migrate manually created Azure resources into Terraform?

--> How do multiple Terraform modules communicate with each other using outputs, variables, and

--> Have you created custom Terraform modules or built infrastructure from scratch?

--> How do you design reusable Terraform modules for multiple environments (Dev, UAT, Prod), and

--> how can a single module be reused across all environments? What are Terraform Workspaces, why are they used, what are their benefits, and when would you

--> use them instead of separate environment folders? How would you plan and execute a Terraform version upgrade (e.g., from 0.x to 1.x), and what

--> compatibility checks would you perform before upgrading?

--> What is the purpose of null_resource in Terraform, and can you give a practical use case?


--> What is a Terraform data block, and how do you use it to reference existing Azure resources in your code?

--> If one VM becomes corrupted after deployment, how would you redeploy only that VM using Terraform?

--> If the Terraform state file gets corrupted, how would you recover it, and what are the best practices to protect the state file?

--> How would you import existing Azure resources into Terraform so they can be managed as Infrastructure as Code?

--> How do you structure your Terraform projects using root and child modules, and do you build your own modules or reuse existing ones?

--> How would you structure your Terraform code using root modules, child modules, locals.tf, for_each, and tfvars to follow the DRY (Don't Repeat Yourself) principle, so that common resources (Key Vault, Recovery Services Vault, Resource Group, etc.) don't need to be defined for every VM?

--> Which Terraform meta-arguments support key-value pairs, and when would you use for_each, count, map, or nested maps?

--> Suppose a resource was created manually in Azure. How will you manage it using Terraform?

--> What is Terraform Drift, and how do you detect and safely handle manual changes made through the Azure Portal before running terraform plan and terraform apply?

--> Suppose an application team requests a new VM. What would be your end-to-end approach for provisioning the VM using reusable Terraform modules and handing it over to the application team?

--> How do you design reusable Terraform modules for multiple environments (Dev, UAT, Prod)?

--> What are Terraform Workspaces, why used, benefits, when instead of separate environment folders?

--> How would you plan and execute a Terraform version upgrade (e.g., 0.x to 1.x), compatibility checks?

--> What is the purpose of null_resource in Terraform, practical use case?

--> What is a Terraform data block, how do you use it to reference existing Azure resources?

--> If the Terraform state file gets corrupted, how would you recover it, best practices to protect state file?

--> How do you securely manage sensitive information (passwords, keys, certs) in Terraform, integrate Azure Key Vault?

--> How would you import existing Azure resources into Terraform?

--> What is Terraform Drift, how detect and safely handle manual changes made through Azure Portal before plan/apply?

--> How do you structure Terraform projects using root and child modules, build own or reuse existing?

--> Application team requests a new VM - end-to-end approach for provisioning using reusable Terraform modules and handing it over.

--> How structure Terraform code using root modules, child modules, locals.tf, for_each, tfvars to follow DRY principle?

--> Which Terraform meta-arguments support key-value pairs - for_each, count, map, nested maps?
