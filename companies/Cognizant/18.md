# 18 Questions at Cognizant

* **Question:** How do you design reusable Terraform modules for multiple environments (Dev, UAT, Prod)?

* **Question:** What are Terraform Workspaces, why used, benefits, when instead of separate environment folders?

* **Question:** How would you plan and execute a Terraform version upgrade (e.g., 0.x to 1.x), compatibility checks?

* **Question:** What is the purpose of null_resource in Terraform, practical use case?

* **Question:** What is a Terraform data block, how do you use it to reference existing Azure resources?

* **Question:** If the Terraform state file gets corrupted, how would you recover it, best practices to protect state file?

* **Question:** How do you securely manage sensitive information (passwords, keys, certs) in Terraform, integrate Azure Key Vault?

* **Question:** How would you import existing Azure resources into Terraform?

* **Question:** What is Terraform Drift, how detect and safely handle manual changes made through Azure Portal before plan/apply?

* **Question:** How do you structure Terraform projects using root and child modules, build own or reuse existing?

* **Question:** Application team requests a new VM - end-to-end approach for provisioning using reusable Terraform modules and handing it over.

* **Question:** How structure Terraform code using root modules, child modules, locals.tf, for_each, tfvars to follow DRY principle?

* **Question:** Which Terraform meta-arguments support key-value pairs - for_each, count, map, nested maps?

* **Question:** VM suddenly loses connectivity to Storage Account over Private Endpoint after Terraform/network change - troubleshoot Private DNS Zone, NSGs, UDRs, firewall rules, port 443.

