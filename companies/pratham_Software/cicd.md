

Q1. Let's take another example. Suppose you have a hundred microservices and you need to write the pipeline for all of them, but there are some stages that are repeated again and again. How would you achieve code reusability?

Q2. How do you integrate the pipeline template? Let's say you have created a template and you need to use that template in 10 different services using YAML. How will you integrate that?

Q3. Suppose you have a multi-stage pipeline where you build once and deploy/promote the same image to multiple environments. You have promoted the image to Dev and UAT before moving to Production. I want you to incorporate an approval stage. How will you do that?

Q4. How do you secure your pipeline variables?

Q5. The default agent/runner is busy doing some other task, and you have an urgent pipeline execution for which you need to use your custom runner/custom agent. How would you integrate that, and how would you ensure your pipeline runs on that custom agent/runner?