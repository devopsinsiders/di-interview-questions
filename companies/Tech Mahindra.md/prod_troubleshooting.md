Q. Suppose an application is built using a pipeline and deployed to a target VM. If the application package is a .jar executable file, how would you install and run it on a Linux VM manually? What type of role or permissions are required on the Linux VM to perform the installation, and would those permissions be assigned to an individual or a group?

Q. Suppose a production application gets stuck or hangs. What would be your initial analysis and troubleshooting steps on the Linux VM?

Q. What is your scripting experience with PowerShell and Bash?