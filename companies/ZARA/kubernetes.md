

Q.  In Kubernetes, what’s the difference between a Deployment and a StatefulSet, and when would you choose one over the other?

Q. At a high level, what does Kubernetes provide that Docker alone doesn’t when you’re running containers in production?

Q. If a container keeps restarting in Kubernetes with CrashLoopBackOff, what are the first two things you would check to identify the root cause?

Q. Conceptually, what problem does a Kubernetes Service solve for pods, and what’s the difference between ClusterIP and LoadBalancer?