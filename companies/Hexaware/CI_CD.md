



Q. Can you explain the end-to-end CI/CD flow?
Q. If I want to create a CI/CD flow in Azure DevOps for Terraform infrastructure, how would it be?
Q. Why can't we have a single pipeline across all the environments?
Q. What is the limitation of having a single pipeline?
Q. If I have a general pipeline in Azure DevOps and I have created a Terraform template, how would I consume that particular template into the current build pipeline or future build pipeline?
Q. Can you write the code?
Q. If I want to design a pipeline such that when I run the pipeline, it gives me options about what to provision — for example, which server to provision or which server to destroy — how would I do that?
Q. Which feature would allow me to do that customization?
Q. If I have a build stage where I am doing Terraform fmt and validate, and the client says don't use the predefined plugins provided by Azure; instead use command-line tools or PowerShell/Shell scripts, how would you do that?
Q. Which task would you use for that?
Q. If I don't want to use the predefined Terraform task and instead want to use the Terraform command-line tool, how would I do that in the pipeline?
Q. Which task would you use to execute those commands?
Q. If you are comfortable with Python or Bash, which task/tool would you use to execute those scripts?
Q. Instead of using the predefined Terraform task, I want to use a command-line task where I can provide the full-fledged command. How would I do that?
Q. What is the difference between this predefined Terraform task and the command-line approach?