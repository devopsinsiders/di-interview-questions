# CI/CD Questions at Wissen Technology

* **Question:** Have you created CI/CD pipelines?

* **Question:** Which CI/CD tools are you familiar with?

* **Question:** Write a GitHub Actions pipeline to deploy Terraform code to Azure.

* **Question:** Which runner will you use in GitHub Actions?

* **Question:** What pre-installation/setup steps are required before running Terraform in a pipeline?

* **Question:** Why is a runner required?

* **Question:** Where do you write commands like `terraform init` and `terraform apply` in a GitHub Actions

* **Question:** workflow? What is the difference between `uses` and `run` in GitHub Actions?

* **Question:** Terraform When creating a Resource Group, VNet, Subnet, NIC, and VM, how does Terraform know the

* **Question:** correct creation order? What is `depends_on` in Terraform?

* **Question:** What Terraform modules have you worked on?

* **Question:** Create an identical Azure VM using Terraform, and write out the folder structure and scripts.

* **Question:** How do you import an existing Azure VM into Terraform?

* **Question:** How many variable types exist in Terraform?

* **Question:** Write examples of List, Set, Map, and Object variables in Terraform.

* **Question:** How do you handle multiple Azure Subscription IDs in Terraform?

* **Question:** In main.tf, how do you pass multiple Subscription IDs to a module?

* **Question:** Why can't a child module directly access variables from the root module?

* **Question:** How do you create multiple Azure VMs with the same configuration?

* **Question:** What is the difference between `count` and `for_each`?

* **Question:** How do you prevent accidental deletion of production VMs?

* **Question:** Which Terraform command performs a dry run and dependency check?

* **Question:** How does Terraform identify resource dependencies?

* **Question:** What is `depends_on` and when do you use it?

* **Question:** How do you protect production resources in Terraform? Show a lifecycle block.

* **Question:** Write a Terraform import command for any Azure resource.

* **Question:** Python & Scripting What Python modules have you worked with?

* **Question:** Read JSON data and print all its values.

* **Question:** Print only the selected candidate's Name, Age, and Skills based on the `is_selected` field.

* **Question:** 93

